import React from 'react'
import ReactDOM from 'react-dom/client'
import './main.css'
import RoomSchedCaller from "./RoomSchedCaller.tsx";

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RoomSchedCaller/>
  </React.StrictMode>,
)
